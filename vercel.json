{
  "buildCommand": "npm install dotenv && npm run build",
  "outputDirectory": "dist/public",
  "env": {
    "NODE_OPTIONS": "--no-deprecation"
  },
  "rewrites": [
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/expiry",
      "schedule": "14 20 * * *",
      "description": "00:00 Vietnam time (UTC+7)"
    }
  ]
}